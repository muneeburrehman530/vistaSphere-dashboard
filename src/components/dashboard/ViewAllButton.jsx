import { Button } from "@mui/material";

const ViewAllButton = () => {
  return (
    <Button varient="contained" className="view-all-button">
      View All
    </Button>
  );
};

export default ViewAllButton;
